# Client-side (public, baked into JS bundle)
VITE_CHANNEL_NAME=badvf
VITE_TWITCH_CLIENT_ID=rfht3kdwifzsiw4baj1j7fairifr6f
# Optional override. If omitted, the app uses `${window.location.origin}/auth/callback`
VITE_TWITCH_REDIRECT_URI=https://badvf-secasino.vercel.app/auth/callback

# Server-side only (never exposed to browser)
CHANNEL_NAME=badvf
SE_JWT=your_streamelements_jwt_token_here
